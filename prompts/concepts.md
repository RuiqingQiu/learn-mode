Tag this exchange with the concepts it is actually about.

Return 1–3 short labels. A label names a specific, reusable concept — something
the same person could plausibly collide with again in a different question.

- Good: "go defer scoping", "python mutable default arguments", "postgres index
  selectivity", "react effect cleanup".
- Too broad: "go", "python", "databases", "react".
- Too narrow: "deferring file.Close inside a loop over 10000 files".

Lowercase, 2–5 words, no punctuation. Prefer an existing phrasing over a novel
one — these labels are matched across exchanges, so "python mutable default
arguments" and "mutable defaults in python" being two different tags is a
failure.
